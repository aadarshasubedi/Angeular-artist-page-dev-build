import { DataTiltDirective } from './data-tilt.directive';

describe('DataTiltDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTiltDirective();
    expect(directive).toBeTruthy();
  });
});
