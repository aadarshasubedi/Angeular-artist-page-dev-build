import { VipJoinDirective } from './vip-join.directive';

describe('VipJoinDirective', () => {
  it('should create an instance', () => {
    const directive = new VipJoinDirective();
    expect(directive).toBeTruthy();
  });
});
